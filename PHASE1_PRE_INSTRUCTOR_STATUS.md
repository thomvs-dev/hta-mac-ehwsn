# HTA-MAC Phase 1 Inspection Status

Status: **PASS** on 2026-07-28.

Definitive command:

```powershell
$env:PYTHONPATH='F:\WSN\matlab\stage2\hta-mac'
python -B validation\run_phase1_gate_scheduled.py
```

Printed gate evidence:

- Frozen HEART-CH heads at reset: 5
- 20-round maximum energy-conservation error: 0.000e+00 J
- Same-seed 20-round trajectory: deterministic
- Budget projection: 0 violations in 1000 random cases
- Solar KS test (10,000 samples): D=0.0116, p=0.511606
- Thermal KS test (10,000 samples): D=0.0157, p=0.169934
- Static-TDMA median T_FND: idle on 72 rounds, idle off 819 rounds
- Paired median shift: 747 rounds

The idle comparison uses the full member and CH Tx/Rx/aggregation/idle
accounting implemented in `IntraClusterMACEnv`. Both arms consume the same
per-round CH schedule generated by the immutable HEART-CH checkpoint; the CH
model is not retrained and routing is not modified.

The 100-slot gate ceiling only ensures static TDMA feasibility for any
realized cluster. It is not a final claim that T=100 is optimal. The protocol
budget remains a sweepable HTA-MAC design parameter.

The large idle-on degradation is a mechanism-validation result, not a protocol
superiority result. It follows the mandated convention `P_idle=E_elec` and one
packet slot equals 4000 bit-times. That convention must be retained or
explicitly sensitivity-tested before publication interpretation.

Machine-readable evidence:
`outputs/logs/phase1_gate.json`.
