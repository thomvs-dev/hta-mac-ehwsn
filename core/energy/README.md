# Frozen radio-energy model

This directory is reserved for an exact adapter around HEART-CH's first-order
radio model. The new idle-listening model belongs to Phase 1 and must not be
implemented until the Phase 0 gate passes.

